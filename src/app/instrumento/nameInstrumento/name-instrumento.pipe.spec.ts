import { NameInstrumentoPipe } from './name-instrumento.pipe';

describe('NameInstrumentoPipe', () => {
  it('create an instance', () => {
    const pipe = new NameInstrumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
